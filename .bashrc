# ~----- Keep it simple shabab -----~

# ~----- Aliases
# Misc
alias v='vim'
alias q='exit'

# Git
alias g='git'
alias gst='git status'
alias gcan='git commit --amend --no-edit'

# Tmux
alias t='tmux'
alias tks='tmux kill-server'

# ~------ Colors
export CLICOLOR=1
export LSCOLORS=ExFxBxDxCxegedabagacad
alias ls='ls -GFh'

# ~----- Prompt
export PS1="\n\w\n"

# ~----- End for now --------------~
